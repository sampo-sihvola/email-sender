## what is this?

python script that reads email addresses from a .csv file and sends an email to every address in the file

csv column titles: "topic" & "email"

copy a .csv file into the folder and run the script

script works only on outlook (classic)

## requirements

``pip install csv``

``pip install win32``